export default function Deals() {
  return (
    <main className="max-w-md mx-auto p-6 pb-24" style={{ paddingBottom: 'calc(6rem + env(safe-area-inset-bottom))' }}>
      <h1 className="font-display text-2xl mb-2" style={{ color: 'var(--text)' }}>Gutscheine & Angebote</h1>
      <p style={{ color: 'var(--text)', opacity: 0.8 }}>Hier findest du bald Gutscheine, Aktionen und exklusive Deals.</p>
    </main>
  );
}
